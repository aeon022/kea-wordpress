%%SELECTOR%% {
    width: {{ design.dimensions.width.style }};

    --items-width: {{ design.items_style.width.style }};
    --items-height: {{ design.items_style.height.style }};
    --gap: {{ design.dimensions.viewport_gap.style }};

    {{ macros.spacing_margin_all(design.margin) }}
    {{ macros.background(design.background) }}
}