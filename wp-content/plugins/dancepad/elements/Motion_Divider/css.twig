%%SELECTOR%%{
    {{ macros.spacing_margin_all(design.margin) }}

    width: {{ design.dimensions.width.style }}; /**/
    max-width: {{ design.dimensions.max_width.style }};
}
%%SELECTOR%% .dan-motion-divider__motion{
    --height: {{ design.dimensions.height.style }};
    border-top-style: {{ design.type.type }};

    border-color: {{ design.colors.color }};
}