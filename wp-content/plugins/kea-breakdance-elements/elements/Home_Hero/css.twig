%%SELECTOR%% .kea-home-hero__eyebrow{ {{ macros.typography(design.typography.eyebrow, globalSettings) }} }
%%SELECTOR%% h1{ {{ macros.typography(design.typography.title, globalSettings) }} }
%%SELECTOR%% .kea-home-hero__text{ {{ macros.typography(design.typography.text, globalSettings) }} }
%%SELECTOR%% .kea-home-hero__actions a{ {{ macros.typography(design.typography.buttons, globalSettings) }} }
