%%SELECTOR%% .kea-destination-list__header p{ {{ macros.typography(design.typography.eyebrow, globalSettings) }} }
%%SELECTOR%% .kea-destination-list__header h2{ {{ macros.typography(design.typography.title, globalSettings) }} }
%%SELECTOR%% .kea-destination-list__header>div:last-child{ {{ macros.typography(design.typography.intro, globalSettings) }} }
%%SELECTOR%% .kea-destination-list__meta{ {{ macros.typography(design.typography.card_meta, globalSettings) }} }
%%SELECTOR%% .kea-destination-list__content strong{ {{ macros.typography(design.typography.card_title, globalSettings) }} }
%%SELECTOR%% .kea-destination-list__content span:last-child{ {{ macros.typography(design.typography.card_link, globalSettings) }} }
